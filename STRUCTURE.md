# Project Structure
gateway/               # Native Python Proxy (FastAPI)
central-service/       # Dockerized Backend (Postgres, MinIO, Model Server, Dashboard)
guardrail-config/      # Configuration and BYOC rules
docs/                  # Documentation
